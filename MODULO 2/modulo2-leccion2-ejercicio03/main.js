'use strict';

const priceKiwi = 5;
const weightKiwi = 2;
const totalKiwi = (priceKiwi * weightKiwi);



const pricePear = 2;
const weightPear = 3;
const totalPear = (pricePear * weightPear);


const priceGrape = 4;
const weightGrape = 0.5;
const totalGrape = (priceGrape * weightGrape);

console.log(totalKiwi + totalPear + totalGrape);